/*=========================================================
001 RIO MADEIRA • CONFIGURAÇÃO
TCE-RO • ESTAÇÃO 15400000 • PORTO VELHO
Fonte de acompanhamento: SIPAM
=========================================================*/
let RM_DADOS=[]
let RM_CICLOS=[]
let RM_CHEIAS=[]
let RM_SECAS=[]
let RM_CURVA=[]
let RM_GRAFICO_HISTORICO=null
let RM_GRAFICO_EXTREMOS=null
let RM_CARREGADO=false
function rmCliente(){return window.clientPublic||window.client||window.supabaseClient||null}
function rmNumero(v){if(v===null||v===undefined||v==='')return null;let n=Number(v);return Number.isFinite(n)?n:null}
function rmCmParaMetros(v){let n=rmNumero(v);return n===null?null:n/100}
function rmMetros(v,d=2){let n=rmNumero(v);return n===null?'—':`${n.toFixed(d).replace('.',',')} m`}
function rmCentimetros(v,d=0){let n=rmNumero(v);return n===null?'—':`${n.toFixed(d).replace('.',',')} cm`}
function rmDataBR(v){if(!v)return'—';let p=String(v).slice(0,10).split('-');return p.length===3?`${p[2]}/${p[1]}/${p[0]}`:String(v)}
function rmDataISO(v){return v?String(v).slice(0,10):''}
function rmEscaparHTML(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'","&#039;")}
function rmCicloMaisRecente(){return[...RM_CICLOS].sort((a,b)=>String(b.ciclo_hidrologico).localeCompare(String(a.ciclo_hidrologico)))[0]?.ciclo_hidrologico||''}
function rmCorSituacao(n,m,p10,p90){if(n===null)return{texto:'SEM DADOS',classe:'rmNeutro'};if(p10!==null&&n<=p10)return{texto:'MUITO ABAIXO DO PADRÃO HISTÓRICO',classe:'rmSeca'};if(p90!==null&&n>=p90)return{texto:'MUITO ACIMA DO PADRÃO HISTÓRICO',classe:'rmCheia'};if(m!==null&&n<m)return{texto:'ABAIXO DA MÉDIA HISTÓRICA',classe:'rmAtencao'};return{texto:'DENTRO/ACIMA DA MÉDIA HISTÓRICA',classe:'rmNormal'}}
async function rmBuscarTodos(client,tabela,colunas,ordem){let todos=[];for(let inicio=0;inicio<100000;inicio+=1000){let q=client.from(tabela).select(colunas);if(ordem)q=q.order(ordem,{ascending:true});let{data,error}=await q.range(inicio,inicio+999);if(error)throw error;let lote=data||[];todos.push(...lote);if(lote.length<1000)break}return todos}
async function carregarRioMadeira(forcar=false){
let client=rmCliente();let box=document.getElementById('painelRioMadeiraSituacao');
if(!client){if(box)box.innerHTML='<div class="alerta-vermelho"><b>Rio Madeira indisponível.</b><br>Cliente Supabase não encontrado.</div>';return}
if(RM_CARREGADO&&!forcar){alterarCicloRioMadeira();return}
if(box)box.innerHTML='<div class="rioMadeiraAguardando">🌊 Carregando série histórica do Rio Madeira...</div>'
try{
let[r1,r2,r3,r4,r5]=await Promise.all([
rmBuscarTodos(client,'rio_madeira_niveis','data,ciclo_hidrologico,ano_inicio_ciclo,mes,dia,dia_ciclo,nivel_cm,nivel_m','data'),
rmBuscarTodos(client,'vw_rio_madeira_ciclos','*','data_inicio'),
rmBuscarTodos(client,'vw_rio_madeira_ranking_cheias','*','data'),
rmBuscarTodos(client,'vw_rio_madeira_ranking_secas','*','data'),
rmBuscarTodos(client,'vw_rio_madeira_curva_historica','*','dia_ciclo')]);
RM_DADOS=r1;RM_CICLOS=r2;RM_CHEIAS=[...r3].sort((a,b)=>Number(b.nivel_cm||0)-Number(a.nivel_cm||0));RM_SECAS=[...r4].sort((a,b)=>Number(a.nivel_cm||0)-Number(b.nivel_cm||0));RM_CURVA=[...r5].sort((a,b)=>Number(a.dia_ciclo||0)-Number(b.dia_ciclo||0));RM_CARREGADO=true;
preencherFiltrosRioMadeira();renderKPIsRioMadeira();renderRankingRioMadeira();renderSituacaoRioMadeira();renderExtremosRioMadeira();renderGraficoRioMadeiraHistorico();renderGraficoRioMadeiraExtremos();renderTabelaRioMadeira();
}catch(e){console.error('Erro Rio Madeira:',e);RM_CARREGADO=false;if(box)box.innerHTML=`<div class="alerta-vermelho"><b>Rio Madeira indisponível.</b><br>${rmEscaparHTML(e?.message||e)}</div>`}
}
function preencherFiltrosRioMadeira(){let ciclos=[...new Set(RM_CICLOS.map(x=>x.ciclo_hidrologico).filter(Boolean))].sort((a,b)=>String(b).localeCompare(String(a)));let atual=document.getElementById('rmCicloAtual'),comp=document.getElementById('rmCicloComparacao'),tab=document.getElementById('rmFiltroCicloTabela');let sel=atual?.value;if(!ciclos.includes(sel))sel=rmCicloMaisRecente();if(atual)atual.innerHTML=ciclos.map(c=>`<option value="${rmEscaparHTML(c)}"${c===sel?' selected':''}>CICLO ${rmEscaparHTML(c)}</option>`).join('');if(comp)comp.innerHTML='<option value="">COMPARAR COM...</option>'+ciclos.filter(c=>c!==sel).map(c=>`<option value="${rmEscaparHTML(c)}">${rmEscaparHTML(c)}</option>`).join('');if(tab)tab.innerHTML=ciclos.map(c=>`<option value="${rmEscaparHTML(c)}"${c===sel?' selected':''}>${rmEscaparHTML(c)}</option>`).join('')}
function alterarCicloRioMadeira(){if(!RM_CARREGADO)return;renderSituacaoRioMadeira();renderGraficoRioMadeiraHistorico();renderTabelaRioMadeira()}
function renderKPIsRioMadeira(){let d=[...RM_DADOS].filter(x=>x.data).sort((a,b)=>String(a.data).localeCompare(String(b.data))),u=d.at(-1),ant=d.find(x=>rmDataISO(x.data)===rmDataISO(new Date(new Date(`${rmDataISO(u?.data)}T12:00:00`).getTime()-86400000).toISOString())),nv=rmNumero(u?.nivel_m),na=rmNumero(ant?.nivel_m),v=nv!==null&&na!==null?nv-na:null,cheia=RM_CHEIAS[0],seca=RM_SECAS[0];let e=document.getElementById('rmNivelAtual');if(e)e.innerHTML=`${rmMetros(nv)}<small class="rmKpiData">${rmDataBR(u?.data)}</small>`;e=document.getElementById('rmVariacao24h');if(e)e.innerHTML=v===null?'—':`${v>0?'+':''}${v.toFixed(2).replace('.',',')} m<small class="rmKpiData">últimas 24h</small>`;e=document.getElementById('rmCheiaHistorica');if(e)e.innerHTML=`${rmMetros(cheia?.nivel_m)}<small class="rmKpiData">${rmDataBR(cheia?.data)}</small>`;e=document.getElementById('rmSecaHistorica');if(e)e.innerHTML=`${rmMetros(seca?.nivel_m,3)}<small class="rmKpiData">${rmDataBR(seca?.data)}</small>`;e=document.getElementById('rmCiclosHistoricos');if(e)e.textContent=RM_CICLOS.length}
function renderRankingRioMadeira(){let monta=(lista)=>`<div class="tabelaMunicipiosWrap"><table class="tabelaMunicipios"><thead><tr><th>#</th><th>CICLO</th><th>DATA</th><th>NÍVEL</th></tr></thead><tbody>${lista.slice(0,10).map((x,i)=>`<tr><td><b>${i+1}</b></td><td>${rmEscaparHTML(x.ciclo_hidrologico||'—')}</td><td>${rmDataBR(x.data)}</td><td><b>${rmMetros(x.nivel_m,3)}</b></td></tr>`).join('')}</tbody></table></div>`;let a=document.getElementById('painelRioMadeiraCheias'),b=document.getElementById('painelRioMadeiraSecas');if(a)a.innerHTML=monta(RM_CHEIAS);if(b)b.innerHTML=monta(RM_SECAS)}
function renderSituacaoRioMadeira(){let box=document.getElementById('painelRioMadeiraSituacao');if(!box)return;let ciclo=document.getElementById('rmCicloAtual')?.value||rmCicloMaisRecente(),dados=RM_DADOS.filter(x=>x.ciclo_hidrologico===ciclo).sort((a,b)=>String(a.data).localeCompare(String(b.data))),u=dados.at(-1);if(!u){box.innerHTML='Sem dados para o ciclo selecionado.';return}let h=RM_CURVA.find(x=>Number(x.dia_ciclo)===Number(u.dia_ciclo)),n=rmNumero(u.nivel_cm),m=rmNumero(h?.media_cm),p10=rmNumero(h?.p10_cm),p90=rmNumero(h?.p90_cm),s=rmCorSituacao(n,m,p10,p90),dif=n!==null&&m!==null?(n-m)/100:null;box.innerHTML=`<div style="margin-bottom:10px;padding:9px 12px;border:1px solid #bfdbfe;border-radius:9px;background:#eff6ff;font-size:11px;font-weight:800">Fonte de acompanhamento: SIPAM • Base carregada até <b>${rmDataBR(u.data)}</b> • ${RM_DADOS.length.toLocaleString('pt-BR')} registros</div><div class="rmSituacao ${s.classe}"><div class="rmSituacaoTitulo">${s.texto}</div><div class="rmSituacaoNivel">${rmMetros(u.nivel_m)}</div><div class="rmSituacaoData">${rmDataBR(u.data)} • ciclo ${rmEscaparHTML(ciclo)}</div></div><div class="rmResumoGrid"><div><span>Média histórica do dia</span><b>${rmMetros(rmCmParaMetros(m))}</b></div><div><span>P10 histórico</span><b>${rmMetros(rmCmParaMetros(p10))}</b></div><div><span>P90 histórico</span><b>${rmMetros(rmCmParaMetros(p90))}</b></div><div><span>Diferença da média</span><b>${dif===null?'—':`${dif>0?'+':''}${dif.toFixed(2).replace('.',',')} m`}</b></div></div>`}
function renderExtremosRioMadeira(){let box=document.getElementById('painelRioMadeiraExtremos');if(!box)return;let d=[...RM_DADOS].sort((a,b)=>String(a.data).localeCompare(String(b.data))),c=RM_CHEIAS[0],s=RM_SECAS[0];box.innerHTML=`<div class="rmExtremosGrid"><div class="rmExtremoCard"><span>🌊 CHEIA RECORDE</span><strong>${rmMetros(c?.nivel_m)}</strong><small>${rmDataBR(c?.data)}</small></div><div class="rmExtremoCard"><span>🏜️ SECA RECORDE</span><strong>${rmMetros(s?.nivel_m,3)}</strong><small>${rmDataBR(s?.data)}</small></div><div class="rmExtremoCard"><span>📚 SÉRIE HISTÓRICA</span><strong>${RM_CICLOS.length} ciclos</strong><small>${rmDataBR(d[0]?.data)} a ${rmDataBR(d.at(-1)?.data)}</small></div><div class="rmExtremoCard"><span>📍 ESTAÇÃO</span><strong>15400000</strong><small>Porto Velho • Rio Madeira</small></div></div>`}
function renderGraficoRioMadeiraHistorico(){let canvas=document.getElementById('graficoRioMadeiraHistorico');if(!canvas||typeof Chart==='undefined')return;let ciclo=document.getElementById('rmCicloAtual')?.value||rmCicloMaisRecente(),comparacao=document.getElementById('rmCicloComparacao')?.value||'',mapa=new Map(RM_DADOS.filter(x=>x.ciclo_hidrologico===ciclo).map(x=>[Number(x.dia_ciclo),rmNumero(x.nivel_m)])),mapa2=new Map(RM_DADOS.filter(x=>x.ciclo_hidrologico===comparacao).map(x=>[Number(x.dia_ciclo),rmNumero(x.nivel_m)])),labels=RM_CURVA.map(x=>Number(x.dia_ciclo)),ds=[{label:`Ciclo ${ciclo}`,data:labels.map(d=>mapa.get(d)??null),borderWidth:3,pointRadius:0,tension:.18},{label:'Mediana histórica',data:RM_CURVA.map(x=>rmCmParaMetros(x.mediana_cm)),borderWidth:2,pointRadius:0},{label:'P10 histórico',data:RM_CURVA.map(x=>rmCmParaMetros(x.p10_cm)),borderWidth:1,pointRadius:0,borderDash:[5,5]},{label:'P90 histórico',data:RM_CURVA.map(x=>rmCmParaMetros(x.p90_cm)),borderWidth:1,pointRadius:0,borderDash:[5,5]}];if(comparacao)ds.splice(1,0,{label:`Ciclo ${comparacao}`,data:labels.map(d=>mapa2.get(d)??null),borderWidth:2,pointRadius:0});if(RM_GRAFICO_HISTORICO)RM_GRAFICO_HISTORICO.destroy();RM_GRAFICO_HISTORICO=new Chart(canvas,{type:'line',data:{labels,datasets:ds},options:{responsive:true,maintainAspectRatio:false,plugins:{datalabels:{display:false}},scales:{y:{title:{display:true,text:'Nível (m)'}}}}})}
function renderGraficoRioMadeiraComparacao(){renderGraficoRioMadeiraHistorico()}
function renderGraficoRioMadeiraExtremos(){let c=document.getElementById('graficoRioMadeiraExtremos');if(!c||typeof Chart==='undefined')return;let l=[...RM_CICLOS].filter(x=>x.ciclo_hidrologico).sort((a,b)=>String(a.ciclo_hidrologico).localeCompare(String(b.ciclo_hidrologico)));if(RM_GRAFICO_EXTREMOS)RM_GRAFICO_EXTREMOS.destroy();RM_GRAFICO_EXTREMOS=new Chart(c,{type:'line',data:{labels:l.map(x=>x.ciclo_hidrologico),datasets:[{label:'Máxima do ciclo',data:l.map(x=>rmCmParaMetros(x.maximo_cm)),borderWidth:2,pointRadius:2},{label:'Mínima do ciclo',data:l.map(x=>rmCmParaMetros(x.minimo_cm)),borderWidth:2,pointRadius:2}]},options:{responsive:true,maintainAspectRatio:false,plugins:{datalabels:{display:false}}}})}
function renderTabelaRioMadeira(){let box=document.getElementById('painelTabelaRioMadeira');if(!box)return;let ciclo=document.getElementById('rmFiltroCicloTabela')?.value||document.getElementById('rmCicloAtual')?.value||rmCicloMaisRecente(),lista=RM_DADOS.filter(x=>x.ciclo_hidrologico===ciclo).sort((a,b)=>String(b.data).localeCompare(String(a.data)));box.innerHTML=`<div class="tabelaMunicipiosWrap"><table class="tabelaMunicipios"><thead><tr><th>DATA</th><th>CICLO</th><th>DIA DO CICLO</th><th>NÍVEL CM</th><th>NÍVEL M</th></tr></thead><tbody>${lista.map(x=>`<tr><td>${rmDataBR(x.data)}</td><td>${rmEscaparHTML(x.ciclo_hidrologico||'—')}</td><td>${x.dia_ciclo??'—'}</td><td>${rmCentimetros(x.nivel_cm)}</td><td><b>${rmMetros(x.nivel_m,3)}</b></td></tr>`).join('')}</tbody></table></div>`}
